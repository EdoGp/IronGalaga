class Invader {
	constructor(x, y, height, width, imageUrl, kind) {
		this.image = imageUrl;
		this.x = x;
		this.y = y;
		this.height = height;
		this.width = width;
		this.kind = kind;
	}
	draw() {}
	moveInvader() {}
}
